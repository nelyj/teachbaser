require "spec_helper"

describe Teachbaser do
  it "has a version number" do
    expect(Teachbaser::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
